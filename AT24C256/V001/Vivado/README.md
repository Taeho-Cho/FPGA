***
### AXI_Lite_slv IP
> create an IP from the AXI_Lite_slv file for memery mapping
***
### RTL file tree
```
AT24C256_wrapper.v
└── AT24C256_I2C.sv
    └── prescaler.sv
```
wrap the RTL.sv files to add into the DB
***